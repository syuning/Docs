7.16上线 bug记录

1. 下载示例数据：实例数据下载为空csv
2. 有些弹窗点击旁边可以关掉，有些不可以
9. 还剩7天过期的实例算不算：即将过期；已过期是否生效未验证
11. 异常测试没测过
12. 跳转不到指定tab了

3. tsdb实例名称修改：当实例状态不可用时，无法向后端发请求但是可以改页面上的实例名称(已改）
4. 数据时效不能输入（已改）
5. 全局提示同时最多显示1条（已改）
6. 欠费按需实例下面的横杠（已改）
7. 概览页面的项，空--占位符（已改）
8. 详情页面删除不支持（已改）
10. 概览去掉”台“（已改）